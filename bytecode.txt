add x off       # mem[p+off] += x;
sub x off       # mem[p+off] -= x;
right x         # p += x;
left x          # p -= x;
out off         # putchar(mem[p+off]);
in off          # mem[p+off] = getchar();
open            # while(mem[p]) {
close           # }
clear off       # mem[p+off] = 0;
mul x, y, off   # mem[p+x_off] += mem[p+off] * y;
