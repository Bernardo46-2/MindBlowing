module Consts (
    version,
    memSize,
    memSizeI64
) where

import Data.Int (Int64)

version :: String
version = "I.D.K"

memSize :: Int
memSize = 30_000

memSizeI64 :: Int64
memSizeI64 = 30_000
